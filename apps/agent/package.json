{
  "name": "@playground/agent",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy --config wrangler.jsonc",
    "build": "wrangler deploy --config wrangler.jsonc --dry-run --outdir dist",
    "typecheck": "tsc --noEmit",
    "test": "node --import ./test/platform.mjs --experimental-strip-types --test test/codex.test.ts"
  },
  "dependencies": {
    "@cloudflare/ai-chat": "0.12.0",
    "@cloudflare/sandbox": "0.12.9",
    "@openai/codex-sdk": "0.155.0",
    "agents": "0.23.0",
    "ai": "^7.0.102",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260917.1",
    "@types/node": "^24.13.5",
    "wrangler": "^4.134.0"
  }
}
